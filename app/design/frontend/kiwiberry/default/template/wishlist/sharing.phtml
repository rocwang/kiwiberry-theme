<?= $this->getMessagesBlock()->toHtml() ?>

<div class="page-header">
  <h1><?= $this->__('Share Your Wishlist') ?></h1>
</div>

<form action="<?= $this->getSendUrl() ?>" id="form-validate-share-wishlist" method="post">

  <?= $this->getBlockHtml('formkey') ?>

  <h2><?= $this->__('Sharing Information') ?></h2>

  <div class="form-group">
    <label for="email_address" class="required">
      <em>*</em>
      <?= $this->__('Email addresses, separated by commas') ?>
    </label>
    <textarea name="emails"
              rows="5"
              id="email_address"
              class="form-control validate-emails required-entry"><?php
      echo $this->getEnteredData('emails')
      ?></textarea>
  </div>

  <div class="form-group">
    <label for="message"><?= $this->__('Message') ?></label>
    <textarea id="message"
              name="message"
              class="form-control"
              rows="5"
      ><?= $this->getEnteredData('message') ?></textarea>
  </div>

  <?php if ($this->helper('wishlist')->isRssAllow()): ?>
    <div class="form-group">
      <label for="rss_url">
        <input type="checkbox"
               name="rss_url"
               id="rss_url"
               value="1"
               title="<?php
               echo $this->__('Check this checkbox if you want to add a link to an rss feed to your wishlist.') ?>"
          >
        <?= $this->__('Check this checkbox if you want to add a link to an rss feed to your wishlist.') ?>
      </label>
    </div>
  <?php endif; ?>

  <p class="text-danger"><?= $this->__('* Required Fields') ?></p>

  <div>
    <a href="<?= $this->getBackUrl(); ?>" class="btn btn-default">
      <i class="fa fa-angle-double-left"></i>
      <?= $this->__('Back') ?>
    </a>

    <button type="submit" title="<?= $this->__('Share Wishlist') ?>" class="btn btn-default">
      <i class="fa fa-share-alt"></i>
      <?= $this->__('Share Wishlist') ?>
    </button>
  </div>
</form>
