<div class="page-header">
  <h1><?= $this->__('Catalog Advanced Search') ?></h1>
</div>



<?php if ($this->getResultCount()): ?>

  <p class="text-info">
    <?php
    echo $this->helper('catalogsearch')
      ->__('<strong>%d item(s)</strong> were found using the following search criteria', $this->getResultCount());
    ?>
  </p>

<?php else: ?>

  <p class="text-danger">
    <?= $this->helper('catalogsearch')->__('No items were found using the following search criteria.'); ?>
    <a href="<?= $this->getFormUrl(); ?>" class="btn btn-default">
      <?= $this->helper('catalogsearch')->__('Modify your search'); ?>
    </a>
  </p>

<?php endif; ?>

<div class="row">
  <?php foreach (array('left', 'right') as $side): ?>
    <?php $searchCriterias = $this->getSearchCriterias(); ?>
    <?php if (isset($searchCriterias[$side])): ?>

      <div class="col-sm-6">
        <ul>
          <?php foreach ($searchCriterias[$side] as $criteria): ?>
            <li>
              <strong><?= $this->escapeHtml($this->helper('catalog')->__($criteria['name'])); ?>:
              </strong> <?= $this->escapeHtml($criteria['value']); ?>
            </li>
          <?php endforeach; ?>
        </ul>
      </div>

    <?php endif; ?>
  <?php endforeach; ?>
</div>

<?php if ($this->getResultCount()): ?>
  <p class="text-info">
    <?= $this->helper('catalogsearch')->__("Don't see what you're looking for?"); ?>
    <a href="<?= $this->getFormUrl(); ?>" class="btn btn-default">
      <?= $this->helper('catalogsearch')->__('Modify your search'); ?>
    </a>
  </p>
<?php endif; ?>

<?php if ($this->getResultCount()): ?>
  <?= $this->getProductListHtml() ?>
<?php endif; ?>
